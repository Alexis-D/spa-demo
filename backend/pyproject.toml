[project]
name = "spademo"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi[standard]>=0.141.1",
]

[tool.fastapi]
entrypoint = "spademo.main:app"

[build-system]
requires = ["uv_build>=0.11.27,<=0.12.7"]
build-backend = "uv_build"
