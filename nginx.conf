resolver 127.0.0.11 ipv6=off;

rewrite_log on;
error_log   /var/log/nginx/error.log debug;

server {
    listen 80;
    server_name foo.com;

    location ~ ^/api(/.*)$ {
        proxy_pass http://api:8000$1$is_args$args;
    }

    root /dist;
}
